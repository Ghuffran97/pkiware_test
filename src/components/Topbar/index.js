import React from "react";
import { Layout } from "antd";

const { Header } = Layout;

const TopBar = ({ background }) => {
  return <Header style={{ padding: 0, background: background }}>










    
  </Header>;
};

export default TopBar;
